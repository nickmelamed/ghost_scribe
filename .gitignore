.venv/
__pycache__/
*.pyc
.DS_Store

# Raw/large data — not committed, see data/download.py
data/raw/
data/interim/
data/processed/*.parquet
data/processed/*.csv

# Model artifacts
stage2/adapters/
stage2/data/
stage1/models/*.joblib
*.bin
*.safetensors

# Kaggle credentials — never commit
.kaggle/
kaggle.json

# HITL review session output — local, not committed
hitl/reviews.jsonl
